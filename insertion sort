def insertinonsort(arr):
    n=len(arr)
    for i in range(1,n):
        key=arr[i]
        j=i-1
        while(j>=0 and arr[j]>key):
            arr[j+1]=arr[j]
            j=j-1
            arr[j+1]=key
    
arr=[5,1,7,2,8,3,4,9,6]
insertinonsort(arr)
print(arr)
    
